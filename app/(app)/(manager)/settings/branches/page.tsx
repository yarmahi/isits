/** Placeholder until Phase B (branches CRUD). */
export default function SettingsBranchesPage() {
  return (
    <div className="space-y-2">
      <h2 className="text-lg font-semibold tracking-tight">Branches</h2>
      <p className="text-sm text-muted-foreground">
        Manage branch locations for intake records. Table, modals, and
        pagination are added in the next phase.
      </p>
    </div>
  );
}
