/** Placeholder until Phase D (delivery methods CRUD). */
export default function SettingsDeliveryMethodsPage() {
  return (
    <div className="space-y-2">
      <h2 className="text-lg font-semibold tracking-tight">
        Delivery methods
      </h2>
      <p className="text-sm text-muted-foreground">
        Manage how equipment is delivered. Full CRUD follows in a later phase.
      </p>
    </div>
  );
}
