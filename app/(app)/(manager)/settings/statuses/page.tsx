/** Placeholder until Phase C (statuses CRUD). */
export default function SettingsStatusesPage() {
  return (
    <div className="space-y-2">
      <h2 className="text-lg font-semibold tracking-tight">Statuses</h2>
      <p className="text-sm text-muted-foreground">
        Manage workflow statuses. Full CRUD follows in a later phase.
      </p>
    </div>
  );
}
